# Profile Repository Notes

This repository should be named `jost-nfg` on GitHub.

GitHub shows the root `README.md` from a repository with the same name as the account on the profile page.

Recommended next steps:

1. Create or open `https://github.com/jost-nfg/jost-nfg`.
2. Push this folder to that repository.
3. Pin the strongest repositories on the profile:
   - `rechner`
   - `plotter`
   - `roboter`
   - `SystemSentinel`

